import type { AnyRecord } from '../../utils/record-formatters'

export type { AnyRecord }

export type TaskCounts = {
  doing: number
  todo: number
  backlog: number
  done: number
  total: number
}
