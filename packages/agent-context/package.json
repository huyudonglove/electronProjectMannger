{
  "name": "@electron-manager/agent-context",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @electron-manager/agent-core build && pnpm build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@electron-manager/agent-core": "workspace:*",
    "@electron-manager/agent-prompts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.2"
  }
}
