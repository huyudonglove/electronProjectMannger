{
  "name": "@electron-manager/agent-project-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @electron-manager/agent-core build && pnpm --filter @electron-manager/project-core build && pnpm build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@electron-manager/agent-core": "workspace:*",
    "@electron-manager/project-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.2"
  }
}
